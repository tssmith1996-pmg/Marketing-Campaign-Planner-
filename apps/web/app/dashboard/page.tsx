
export default async function Dashboard() {
  // Simple placeholder; actual data fetched from /api/campaigns
  return (
    <main style={{ padding: 24 }}>
      <h2>Dashboard</h2>
      <p>This is a starter dashboard. Hook up CampaignTable to /api/campaigns.</p>
    </main>
  );
}
